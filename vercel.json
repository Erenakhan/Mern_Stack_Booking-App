{
    "buildCommand": "cd client && yarn install && ./node_module/vite/bin/vite.js build",
    "outputDirectory":"client/dist",
    "framework": "vite",
    "rewrites":[
        {
        "source":"/server/(.*)",
        "destination":"/server/index.js"
     }
    ]
}